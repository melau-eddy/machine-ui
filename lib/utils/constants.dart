class AppColors {
  static const primaryBackground = Color(0xFF0A0E21);
  static const cardBackground = Color(0xFF1A1E3A);
  static const accentColor = Colors.cyan;
  // Add other colors
}

class AppTextStyles {
  static TextStyle headline = GoogleFonts.orbitron(
    fontSize: 24,
    fontWeight: FontWeight.w600,
    color: Colors.white,
  );
  // Add other text styles
}